name: fluttercon_eu_2025
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  animated_to: ^0.4.0
  flutter:
    sdk: flutter
  flutter_deck: ^0.21.0
  flutter_inappwebview: ^6.1.5
  gap: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
